{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"], // when I press `cmd + s` to fix it, the comma won't disappear, and the error will still exist
    },
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"],
}
